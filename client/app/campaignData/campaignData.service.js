'use strict';

angular.module('bApp')
  .service('campaignData', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
       var data = {
     //  	'title' = ''
       };
    return data;
  });
